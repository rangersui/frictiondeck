# map — world index

default              — scratch pad, general workspace
map                  — world index (this file)
sys-health           — system vitals, 60s heartbeat (auto, renderer-health)
config-actions       — plugin route whitelist (auto-managed)
skills-core          — protocol reference (auto-sync from skills/)
skills-data          — data/view separation patterns (auto-sync)
skills-renderer      — renderer spec and composability (auto-sync)
skills-patch         — dom_patch decision tree (auto-sync)
skills-security      — CSP, auth, HMAC constraints (auto-sync)
skills-translate     — translate plugin and ingest pipeline (auto-sync)
renderer-health      — health dashboard renderer (auto-sync)
renderer-email       — email viewer renderer
plugin-proposals     — AI plugin proposals awaiting approval
fine-tune            — fine-tuning notes and config
email                — email workspace
acceleration-stack   — inference acceleration research
speculative-decoding — speculative decoding experiments
teacher              — teaching/training workspace
